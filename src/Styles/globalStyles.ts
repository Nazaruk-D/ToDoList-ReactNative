import {StyleSheet} from "react-native";

export const globalStyles = StyleSheet.create({
    border: {
        borderStyle: 'solid',
        borderWidth: 1,
        borderColor: 'red',
    },
    center: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    }
})